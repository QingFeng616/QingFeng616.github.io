# 2026-07-23 工作日志

## docsify 个人网站清理

- 项目是用户从 docsify 源码仓库 (docsifyjs/docsify) clone 后用于个人网站的
- 网站内容在 `docs/` 目录，通过 CDN 加载 docsify@4，不依赖本地源码
- 用户的 GitHub 仓库: QingFeng616/QingFeng616.github.io
- 删除了约 170 个 docsify 开发源码遗留文件：
  - 源码目录: src/ (74文件), test/ (33文件), build/ (4文件)
  - 开发配置: package.json, babel/eslint/jest/playwright/rollup/postcss 配置, server.js, Dockerfile, vercel.json 等
  - 项目元文件: README.md(根), CHANGELOG.md, HISTORY.md, CONTRIBUTING.md, SECURITY.md, LICENSE
  - 开发工具: .github/, .husky/, .vscode/, .gitpod.yml, .editorconfig 等
  - docs/ 中 18 个 docsify 官方文档 md 文件
  - docs/ 中示例文件 (example.*), 多余 svg (moon/sun/vercel), 文档图片
  - docs/CNAME (内容是 docsify.js.org，非用户域名)
- 简化了 .gitignore，移除 docsify 开发专用条目
- 保留: docs/ 下用户的全部内容、.git/、.gitignore
- 所有删除可通过 git 历史恢复

## 网站界面全面优化 (docs/index.html)

- 学习了 docsify 的 DOM 结构 (.sidebar/.content/.markdown-section/.app-nav/section.cover) 与 CSS 变量 (--theme-color)
- 用 CSS 变量重构了整套样式系统，支持亮/暗双主题
- 新增功能（通过 docsify plugin 钩子实现）:
  - 暗色模式：可切换 + localStorage 记忆 + 跟随系统偏好，无闪烁
  - 阅读进度条（顶部，兼容 .content 与 window 两种滚动容器）
  - 回到顶部按钮
  - 每页页脚（版权 + GitHub 链接）
  - 导航栏主题切换按钮（太阳/月亮 SVG）
- 视觉优化：侧边栏分组/激活态、排版行高与标题层级、代码块/表格/引用/图片、封面渐变、tab、搜索框、复制按钮、翻页、滚动条
- 响应式 + prefers-reduced-motion 适配
- 配置增强：新增 logo:'_media/icon.svg'，补全 prism 语言 (c/makefile/cpp) 高亮
- 校验：本地 http 服务 HTTP 200，node --check 内联脚本全部通过

## 第二轮体验增强 (docs/index.html)

- 标题锚点链接：h2/h3/h4 悬停显示可点击 `#`，平滑滚动 + history.replaceState 不触发路由
- 图片点击放大：引入 docsify 官方 zoom-image 插件；去掉 img:hover 缩放避免冲突（改 cursor:zoom-in）
- 页脚升级：beforeEach 估算中文阅读时长(~400字/分)，afterEach 加入「约 X 分钟阅读」+「编辑此页」GitHub 链接(edit/main/docs/<route.file>)
- SEO/OG 元信息：og:title/description/image、twitter card、theme-color(亮/暗)、author，title 改为「我的空间」
- 统一 CDN：翻页插件 unpkg→jsdelivr
- **修复关键 bug**：搜索插件原路径 `docsify@4/plugins/search.min.js` 返回 404（搜索自第一版重写起就失效），修正为 `docsify@4/lib/plugins/search.min.js`（已验证 200）
- 校验：node --check 通过；zoom-image/search 等全部 CDN 资源 HTTP 200；本地服务 200

## 待用户跟进
- about.md / contact.md 仍是模板占位（[你的名字]、[your-email@example.com]），上线前需填真实信息
- 子目录 _sidebar.md 作用域问题（notes/ 下侧边栏只显示 notes 链接），如需全站统一侧边栏可调整
- 中等优化未做：右侧悬浮 TOC、代码软换行、PWA 清单、中文字体(Noto Sans SC)

## 第三轮：正文内容排版优化 (docs/index.html + README.md)
- 正文排版 CSS：长代码块 max-height:480px 内部滚动(避免长文被代码撑爆)；中文/中英文混排 font-feature-settings:"kern"+"liga"、word-break/overflow-wrap；列表标记(::marker)着色；任务列表自定义复选框(li:has(>input[type=checkbox]))；kbd 按键样式；表格 thead sticky 吸顶(top:60px)；外链自动加 ↗ 图标(.site-footer 除外)；blockquote 微调
- admonition 提示框系统：markdown.prerender 正则把 `::: tip/info/warning/danger/success/details` 转为带配色左侧边与标题的提示框，details 转 <details> 可折叠；含全套暗色样式
- 首页 README.md 改为卡片网格：.card-grid(auto-fill grid) + .card(图标/标题/描述)，覆盖 学习笔记6/博客1/项目1/日记1/关于/联系
- 注意：本轮 Bash 工具不可用(node/python 均返回空+exit1，连 echo 也失败)，仅做静态复查确认改动正确；建议用户本地 `npx docsify-cli serve docs` 或预览面板确认
- 内容用法：写 `::: tip 标题` ... `:::` 生成提示框；`- [ ]` 自动变复选框；`<kbd>` 变按键样式


